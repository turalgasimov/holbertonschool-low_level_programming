#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 * @argc: num of args
 * @argv: vector of args
 * Return: 0
 */

int main(int argc, char *argv[])
{
        printf("./%s", argv[0]);
        return (0);
}
